spring:
  application:
    name: Stockfy

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  messages:
    basename:
      - messages
    encoding: UTF-8

payment-service:
  refund-max-days: 15

jwt:
  secret: test-secret-123
  expire-days: 7
  expire-hours: 12
  expire-minutes: 30

refresh-token:
  expire-days: 20
