spring:
  application:
    name: Stockfy
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  messages:
    basename:
      - messages
    encoding: UTF-8

# Environment variables
payment-service:
  refund-max-days: ${REFUND_MAX_DAYS}